class SortUtils {
  // ascending product sort
  sortProducts(products) {
    // const sortedProducts = products.forEach((a, b) => {
    //   if (a.name[0] > b.name[0]) {
    //     return 1;
    //   } else {
    //     return -1;
    //   }
    // });

    console.log(products);
    // return sortedProducts;
  }
}

export default SortUtils;
